(function(App) {

  App.Collection.Widgets = Backbone.Collection.extend({

    url: '<%= ENV['RW_API_URL'] %>/widget',

    parse: function(data) {
      return data.data;
    }

  });

})(this.App);
